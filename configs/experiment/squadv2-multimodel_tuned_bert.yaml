# @package _global_

# to execute this experiment run:
# python train.py experiment=squadv2-multimodel_tuned_bert trainer=gpu

defaults:
  # load everything from this config file
  - squadv2-multimodel_base.yaml

model:
  aggregate: attention
  pretrained_models:
    bert-base-cased: "bert-base-cased"
